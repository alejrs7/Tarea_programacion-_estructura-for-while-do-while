#include <stdio.h>

int sumaCubosWhile(int n) {
    int suma = 0, i = 1;
    while (i <= n) {
        suma += i * i * i;
        i++;
    }
    return suma;
}

int main() {
    int n = 3;
    printf("Suma de los cubos: %d\n", sumaCubosWhile(n));
    return 0;
}
