#include <stdio.h>

int sumaImparesWhile(int n) {
    int suma = 0, i = 1;
    while (i <= n) {
        suma += i;
        i += 2;
    }
    return suma;
}

int main() {
    int n = 10;
    printf("Suma de los impares: %d\n", sumaImparesWhile(n));
    return 0;
}
