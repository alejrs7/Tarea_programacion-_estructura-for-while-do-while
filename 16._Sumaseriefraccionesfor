#include <stdio.h>

int main() {
    int n;
    double sum = 0.0;

    printf("Introduce un número: ");
    scanf("%d", &n);

    for (int i = 1; i <= n; i++) {
        sum += 1.0 / i;
    }

    printf("La suma de la serie es: %.2f\n", sum);
    return 0;
}
