#include <stdio.h>

int sumaAritmeticaWhile(int n) {
    int suma = 0, i = 1;
    while (i <= n) {
        suma += i;
        i++;
    }
    return suma;
}

int main() {
    int n = 10;
    printf("Suma de la serie aritmética: %d\n", sumaAritmeticaWhile(n));
    return 0;
}
