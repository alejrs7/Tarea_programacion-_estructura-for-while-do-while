#include <stdio.h>

int sumaCubosDoWhile(int n) {
    int suma = 0, i = 1;
    do {
        suma += i * i * i;
        i++;
    } while (i <= n);
    return suma;
}

int main() {
    int n = 3;
    printf("Suma de los cubos: %d\n", sumaCubosDoWhile(n));
    return 0;
}
