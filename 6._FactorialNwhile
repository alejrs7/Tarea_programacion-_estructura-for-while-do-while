#include <stdio.h>

int factorialWhile(int num) {
    int resultado = 1, i = 1;
    while (i <= num) {
        resultado *= i;
        i++;
    }
    return resultado;
}

int main() {
    int numero = 5;
    printf("Factorial: %d\n", factorialWhile(numero));
    return 0;
}
